# Placeholder secret manifest. DO NOT commit real values — in production,
# generate this via `kubectl create secret` or a secrets manager
# (e.g. Sealed Secrets, External Secrets Operator) instead of a plain YAML.
apiVersion: v1
kind: Secret
metadata:
  name: zeroday-secrets
  namespace: zeroday-platform
type: Opaque
stringData:
  SECRET_KEY: "CHANGE_ME_GENERATE_A_LONG_RANDOM_STRING"
  POSTGRES_USER: "zeroday"
  POSTGRES_PASSWORD: "CHANGE_ME"
